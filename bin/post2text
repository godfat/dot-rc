#!/usr/bin/env ruby

require 'psych'
require 'nokogiri'

d = Psych.load($stdin.read)
puts "time: #{d['time']}\nname: #{d['name']}\n" \
     "body:\n\n#{Nokogiri::HTML.parse(d['body'].
                 gsub('  ', '&#8195;')).root.text}"
