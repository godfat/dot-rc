#/bin/sh

sudo port -N selfupdate

sudo port -N install coreutils fish git socat vim ripgrep

sudo port -N install ruby30 +doc
sudo port -N select --set ruby ruby30

sudo port -N install curl +gnutls +http2 wget +gnutls
sudo port -N install gnupg2
sudo port -N install ImageMagick MPlayer youtube-dl

sudo port -N reclaim
